"""Application version metadata.

App versions are for releases and GitHub tags. They are separate from
db.SCHEMA_VERSION, which only tracks database structure compatibility.
"""

APP_VERSION = '0.7.0'
